import { AsyncSsrManagerV1 } from '..';
import { AsyncSsrManagerContext } from './async-ssr-manager-context';
export declare class AsyncSsrManager implements AsyncSsrManagerV1 {
    private readonly context;
    private readonly timeout?;
    private readonly asyncOperations;
    constructor(context: AsyncSsrManagerContext, timeout?: number | undefined);
    renderUntilCompleted(render: () => string): Promise<string>;
    scheduleRerender(asyncOperation?: Promise<unknown>): void;
    private renderingLoop;
}
