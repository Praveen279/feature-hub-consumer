/**
 * Prepends the given `base` - which can be relative or absolute - to the `url`,
 * unless the `url` is absolute.
 */
export declare function prependBaseUrl(base: string | undefined, url: string): string;
